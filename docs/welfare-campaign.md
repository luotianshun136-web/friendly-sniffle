# 福利日活动与首屏联系

## 已发布内容

首页顺序：品牌栏、福利活动与官方联系方式、原首页视频、暹茅传承、女性情感主题、定制流程与承接边界、其他咨询方向、团队动态、页尾联系。

助理微信 `wasd562482` 为主要入口，师傅微信 `Jw512527` 和 WhatsApp `+8619805071031` 同屏展示。复制按钮只复制号码，不自动添加微信；客户按提示备注后到微信添加。私密咨询浮窗从首页首次显示起即可见，不依赖滚动或联系方式区域的可见性；仅在咨询面板打开时收起，关闭后立即恢复。短屏手机紧凑排布活动内容，避免浮窗遮挡联系方式。

八个主主题围绕女性吸金、女性吸睛、女性崛起、异性缘、付出回报、姻缘和合、情感复合和婚姻修复。七个次要方向单独呈现。各主题均以“加助理微信”为主入口，保留带主题的网站私密留言，不自动发送消息。

## 本次活动

- 活动标识：`welfare-day-v1`，一次性活动，不自动重复。
- 开始：北京时间 **2026年9月12日11:32:40**。
- 截止：北京时间 **2026年9月13日11:32:40**。
- 每人一次，免费围绕一个主要问题提供传统测算解读及具体行动建议。
- 客户需在截止前向助理发送好友申请或留言，备注“福利测算”，由助理核对时间。网站不记录或声称微信添加、报名已完成。
- 后续定制或仪式服务不包含在免费权益内，另行说明，自愿选择。传统测算不作确定预测、医疗或具体结果保证。
- 到期自动显示“本次福利活动已结束”，免费报名提示停止，联系方式保留并改为普通评估备注。已及时申请的客户由助理继续核对。

## 技术与回退

新增 `campaigns` 表，仅包含活动ID、开始及结束时间。增量迁移 `0004_amused_nightcrawler.sql` 不修改任何原有业务表。

`GET /api/campaign` 返回 `id`、`startsAt`、`endsAt`、`serverNow`、`status`，使用 `no-store`；没有公开写接口或活动管理后台。页面服务端输出初始状态，客户端基于服务器时间及单调时钟更新，每分钟及页面重新可见时校准。读取失败时隐藏计时、保留联系及重试入口，不生成新的24小时。

`scripts/campaign-start.sql` 仅在记录不存在时插入，重复执行不会刷新时间。修改页面或再次部署也不会重置活动。要开展下一次活动，必须另行确认新的活动安排，不能清空本记录来制造反复倒计时。

新增主题使用独立ID，保留原六个ID的服务端校验和原名称；历史 `sourceTitle` 不改。普通咨询、帖子私信、自动回复与视频暂停卸载机制保留。

本次版本：`269bd6d9-4dc9-4e7d-97f4-4dd6ea241881`；上一版本：`18d7c7bc-640c-456f-b256-834e2d3f21ac`。回退仅切换Worker版本，不删除活动表或重置业务数据库。发布前快照和线上验证结果保存在忽略目录 `.private/campaign-release`，不会上传GitHub。

## 验证记录

- TypeScript、生产构建及12项单元测试通过，包括活动时间边界、单调计时、无效数据、15个新主题与6个旧主题兼容。
- 隔离D1/R2测试通过：统一截止时间、重复启动不重置、只读接口及禁止缓存、页面顺序、打开窗口不创建咨询、来源校验、重试去重、自动回复一次、客户会话隔离、后台来源及退出登录失效。
- 浏览器检查320×568、375×667、390×844、768×1024、1440×900。前三个为模拟手机视口；最小视口所有官方联系方式在首屏内，且无悬浮按钮遮挡。
- 八个主题均可展开；键盘展开收起、切换主题保留草稿、咨询打开时视频元素卸载均已检查。微信复制成功提示及建议备注正确。
- 本地短倒计时实测归零后自动结束，未刷新页面；服务端503时显示无法确认、保留联系方式及重试，不显示虚构时间。
- 线上只读检查通过：活动时间固定、页面顺序正确，13条动态及9条视频封面保留，未登录后台401，首页与账号、原有数据均未改动。
- 未直接覆盖苹果、安卓厂商浏览器真机、系统键盘或系统级断网。浏览器视口测试不能代替真机覆盖。

不新增付费服务、外部字体、跟踪脚本、付款或自动测算能力。

## 首屏浮窗修复（2026年9月12日）

移除联系方式可见时隐藏浮窗的条件，初始服务端HTML直接包含“私密咨询”按钮；仅在咨询面板打开时收起。短屏手机缩短活动区域间距，不缩小联系方式的点击目标。没有数据库迁移，也没有重新执行活动启动脚本。

已检查320×568、375×667、390×844、768×1024、1440×900模拟视口：首屏按钮可见、可点击，不遮挡三个官方联系方式。刷新、向下浏览、回到顶部、咨询打开与关闭均正常；播放中的首页视频在打开咨询时卸载。TypeScript与生产构建通过，线上初始HTML包含浮窗，活动起止时间保持不变。真机浏览器未直接覆盖。

修复版本：`26025a13-42bd-4cee-ab84-862583c314a4`；可回退到本次活动初始版本 `269bd6d9-4dc9-4e7d-97f4-4dd6ea241881`，无需回退数据库。
